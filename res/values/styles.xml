<resources xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <style name="AppBaseTheme" parent="android:Theme.Holo.Light.DarkActionBar">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
    </style>

    <!-- Application theme. -->

    <style name="AppTheme" parent="@android:style/Theme.Holo.Light">
        <item name="android:actionBarStyle">@style/MyActionBar</item>
        <item name="android:actionBarTabTextStyle">@style/TabTextStyle</item>
        <item name="android:actionBarTabStyle">@style/TabStyle</item>
    </style>

    <!-- ActionBar theme -->

    <style name="MyActionBar" parent="@android:style/Widget.Holo.ActionBar">
        <item name="android:background">@color/action_bar_color</item>
        <item name="android:backgroundStacked">@color/action_bar_color</item>
        <item name="android:backgroundSplit">@color/action_bar_color</item>
    </style>
    <!-- action bar tab text -->
    <style name="TabTextStyle" parent="@android:style/Widget.ActionBar.TabText">
        <item name="android:textColor">@color/actionbar_text</item>
        <item name="android:textSize">15sp</item>
        <item name="android:textAppearance">@android:style/TextAppearance.Widget.TextView.PopupMenu</item>
    </style>
    <!-- action bar tab style -->
    <style name="TabStyle" parent="@android:style/Widget.ActionBar.TabView">
        <item name="android:background">@drawable/actionbar_tab_bg</item>
    </style>
    <!-- Drawer theme -->
    <style name="drawerStyle" parent="android:Widget.Holo.Light.ListView">
        <item name="android:background">@color/drawer_background</item>
        <item name="android:choiceMode">singleChoice</item>
        <item name="android:divider">@color/drawer_divider</item>
        <item name="android:dividerHeight">1dp</item>
        <item name="android:listSelector">@color/drawer_selector</item>
    </style>

</resources>